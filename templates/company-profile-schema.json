{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Company Profile",
  "description": "Schema for SBIR/STTR company profile validation",
  "type": "object",
  "required": [
    "company_name",
    "capabilities",
    "certifications",
    "employee_count",
    "key_personnel",
    "past_performance",
    "research_institution_partners"
  ],
  "properties": {
    "company_name": {
      "type": "string"
    },
    "capabilities": {
      "type": "array",
      "items": { "type": "string" }
    },
    "certifications": {
      "type": "object",
      "required": ["sam_gov", "socioeconomic", "security_clearance", "itar_registered"],
      "properties": {
        "sam_gov": {
          "type": "object",
          "required": ["active"],
          "properties": {
            "active": { "type": "boolean" },
            "cage_code": { "type": "string" },
            "uei": { "type": "string" }
          }
        },
        "socioeconomic": {
          "type": "array",
          "items": { "type": "string" }
        },
        "security_clearance": {
          "type": "string",
          "enum": ["none", "confidential", "secret", "top_secret"]
        },
        "itar_registered": { "type": "boolean" }
      }
    },
    "employee_count": {
      "type": "integer"
    },
    "key_personnel": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "role", "expertise"],
        "properties": {
          "name": { "type": "string" },
          "role": { "type": "string" },
          "expertise": {
            "type": "array",
            "items": { "type": "string" }
          }
        }
      }
    },
    "past_performance": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["agency", "topic_area", "outcome"],
        "properties": {
          "agency": { "type": "string" },
          "topic_area": { "type": "string" },
          "outcome": { "type": "string" }
        }
      }
    },
    "research_institution_partners": {
      "type": "array",
      "items": { "type": "string" }
    },
    "sources": {
      "type": "object",
      "properties": {
        "web_references": {
          "type": "array",
          "description": "URLs consulted during profile research phase",
          "items": {
            "type": "object",
            "required": ["url", "label"],
            "properties": {
              "url": { "type": "string" },
              "label": { "type": "string" },
              "accessed_at": { "type": "string", "format": "date-time" }
            }
          }
        },
        "documents_scanned": {
          "type": "array",
          "description": "Local file paths provided during profile creation (capability statements, SAM.gov exports, etc.)",
          "items": {
            "type": "object",
            "required": ["path"],
            "properties": {
              "path": { "type": "string" },
              "label": { "type": "string" },
              "scanned_at": { "type": "string", "format": "date-time" }
            }
          }
        },
        "directories_scanned": {
          "type": "array",
          "description": "Directory paths scanned during corpus ingestion that contributed to this profile",
          "items": {
            "type": "object",
            "required": ["path"],
            "properties": {
              "path": { "type": "string" },
              "file_count": { "type": "integer" },
              "scanned_at": { "type": "string", "format": "date-time" }
            }
          }
        }
      }
    }
  }
}
